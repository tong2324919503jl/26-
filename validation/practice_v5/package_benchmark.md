# 共同 7000 例：冻结候选测试结果

两题各使用原文件中的全部 7000 例。没有根据结果修改算法、参数或挑选案例。

| 项目 | 第三问 | 第四问 |
|---|---:|---:|
| 全部清除案例 | 7000/7000 | 7000/7000 |
| 失败案例 | 0 | 0 |
| 平均秒/源 | 186.1942 | 426.4489 |
| 本组均值低于 220 / 400 秒目标 | 是 | 否 |

计分方法：每例从进入到退出的总虚拟时间除以成功清除源数，再对 7000 例等权平均。每例均需完成覆盖并清除所有源，或实际清除 16 个源；未用概率提前停止。这里的虚拟秒数不是电脑运行耗时。

这只是本地自建仿真在这批固定输入上的成绩，不代表官方平台成绩，也不证明在所有未知分布上达标。不同样本池之间的均值变化不能直接归因于算法改进。

算法在下载并审查这批样本的生成代码之前已冻结。真值只由本地仿真器和计分程序读取，策略使用公开接口；模型未展开样本内容，只读取总体结果。按用户最新要求已停止重叠核对，因此未验证这批样本与所有历史案例是否重复。

GitHub 只读下载，固定提交 `8aefbd78df015bf8d2034257a07353176c35798d`；没有改动远程仓库，没有操作官方模拟器或运行正式测试。全部输出在国赛2026内。

## 复核文件

- `frozen_selection_v5.json`：测试前固定的算法及依赖指纹。
- `results/p3_shared7000_v5.json`、`results/p4_shared7000_v5.json`：总体结果，可直接分享。
- 同名 `.journal.jsonl`：运行起止和完成记录；`.private_rows.jsonl` 是逐例机器记录，本报告未展开读取。
- `shared_test_audit/download_manifest.json`：下载版本及文件指纹。

## 文件完整性

- 问题3：输入 SHA-256 `9a31464bfbc136f683e75155de7a09a91c1dafdf0c48b9186173d9491ad7eda6`；运行前后检查通过。
- 问题4：输入 SHA-256 `3c64ef7c04700ccd470b05be72ab05badc74feff91ba8ec352e3d31ab3d9eed5`；运行前后检查通过。
