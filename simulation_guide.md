# 问题三、四：模拟器测试怎么做

本项目的本地结果来自自建案例。**官方演练与正式成绩，需要在官方模拟器运行后填写。**下面流程依据附件 1、附件 2 和题目附录 3、4。

## 1. 准备

- 从[附件给出的地址下载模拟器](https://pan.baidu.com/s/1P1yfVjY0RufU93XOdzhOLw?pwd=2026)，提取码 `2026`，解压后运行。移动软件时整体移动文件夹。
- 用报名信息注册并登录，全程保持联网；电脑时间与服务器相差不能超过 60 秒。同一账号不能同时在两台设备使用。
- 先在本地运行 `python problem3/solve.py` 和 `python problem4/solve.py`，确认程序能完成自建案例。默认不连接官方平台。

## 2. 先做演练

1. 在模拟器选择“问题3演练测试”或“问题4演练测试”，点击开始，等待准备完成、5 秒倒计时结束，界面显示接口就绪。
2. 在仓库文件夹打开终端，运行对应命令，把 `参赛队号` 替换为当前登录的队号：

   ```powershell
   python problem3/solve.py --online --robot-id 参赛队号
   python problem4/solve.py --online --robot-id 参赛队号
   ```

   每局只运行其中一条。默认地址是 `http://127.0.0.1:2026`；修改过端口时，在命令末尾加 `--base-url http://127.0.0.1:新端口`。模拟器和程序必须在同一台电脑。

3. 等程序退出，保存程序生成的过程记录，并在模拟器查看案例编码、结束原因和程序运行时间。演练结束后界面才显示源总数等真值，**需要手工记录，接口不能获取**。
4. 多做不同案例，先确认清除完整，再比较速度。计算：清除比例 = 清除数 / 界面显示的源总数；平均定位清除时间 = 最终虚拟时间 / 清除数。清除数为 0 时平均值记“无定义”。虚拟时间包括移动、切频道、检测、定位和清除，不能用电脑实际运行秒数替代。

演练不限次数。接口在倒计时、非测试期间和结束后都关闭，连接失败此时属正常现象。短暂断网时保留模拟器并等待重连；程序只对同一请求复用原 ID 重试。若程序报告结果未确认，先查看日志及模拟器状态，不要向同一局盲目重发新动作，也不要用 `/exit` 探测结束原因。

## 3. 再做正式测试

- 演练稳定后，选择对应“正式测试”入口，按同样方式运行。**问题三、问题四各只有 3 次；成功启动就占用一次，手工中止也占次，结束后不能继续原局。**
- 正式测试不公布源总数，因此不要臆填“清除比例 100%”。论文每问各填三行：

  | 测试 | 测试案例编码 | 清除干扰源个数 | 平均定位清除时间（虚拟秒/个） | 程序运行时间（现实秒） |
  | --- | --- | --- | --- | --- |
  | 1 | 待测 | 待测 | 待测 | 待测 |
  | 2 | 待测 | 待测 | 待测 | 待测 |
  | 3 | 待测 | 待测 | 待测 | 待测 |

- 等加密行为日志自动上传完成；从模拟器日志列表导出，**保留原文件名**，放入各问 `results/official/` 并随支撑材料提交。程序自己的 JSONL 日志不能替代官方加密日志。官方日志上传上限为 2 MB，演练时留意大小。

## 4. 时间限制

- 倒计时结束后开放 25 分钟测试窗口；成功 `/enter` 后程序最长运行 20 分钟，以先到者为准。程序读取接口实际剩余时间并保留退出余量；检测的 5 秒是虚拟时间，现实无需等待 5 秒。
- 虚拟时间上限 100 小时。超时、中止或技术异常会关闭接口，结束原因看模拟器界面。
- 按题包，**北京时间 2026 年 9 月 13 日 17:30 后不能启动新测试**，建议 **15:30 前完成全部正式测试**。此前启动的测试可以完成，已排队日志仍会继续上传。

原始说明：[附件 1](materials/problem_b/attachments/attachment_1_simulator_guide.docx)、[附件 2](materials/problem_b/attachments/attachment_2_api_protocol.docx)。
