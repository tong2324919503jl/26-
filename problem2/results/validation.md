# 问题 2 合并验证记录

验证日期：2026-09-10。全部为自建离线数学实验，不是官方数据、模拟器演练或正式测试成绩。

## 合并后的默认结果

默认采用四圆盘收信保证、目标圆条件化、360边圆外包、0.1°读数网格及半格增宽，并统一精度复核548个已访问候选。原解析策略保留为 `choose_second`；默认示例使用 `choose_second_refined`。

| 项目 | 原点向东案例 | 首点(1500,0)向东案例 |
|---|---:|---:|
| 第二点局部坐标/m | (842.143737,546.894537) | (220.941938,195.472914) |
| 第二点世界坐标/m | (842.143737,546.894537) | (1720.941938,195.472914) |
| 连续第二读数下的几何直径上界/m | 112.329304 | 15.768784 |
| 本次移动距离/m | 1004.141279 | 295.000000 |
| 移动与第二检测时间/s | 205.828256 | 64.000000 |
| 自建源世界坐标/m | (1200,15) | (1720,2) |
| 自建接收半径/m | 1300 | 1000 |
| 自建第二点固定误差/° | +0.4 | +0.4 |
| 该次读数的最终后验外包直径/m | 71.126450 | 10.323284 |

源真值和接收半径真值只用于选点完成后的验证，不参与任何候选选择。保证直径、具体读数后的区域直径和真实点估计误差是不同指标。

## 同口径改善与时间折中

所有点均使用相同的目标圆条件化先验、360边圆外包和0.1°读数步长重新评分。完整原始数值、移动预算与精度敏感性见 [strategy_comparison.json](strategy_comparison.json) 和 [strategy_comparison.md](strategy_comparison.md)。

| 场景 | 原仓点的几何上界/m | 队友点的几何上界/m | 融合点的几何上界/m |
|---|---:|---:|---:|
| 原点向东 | 115.693039 | 113.037691 | 112.329304 |
| (1500,0)向东 | 54.023570 | 15.966125 | 15.768784 |

融合点相对原仓点的同口径保证指标分别改善约2.91%和70.81%。原仓的131.221007米是较保守的解析条带上界，不能直接与112.329304米相减后称为真实定位精度提升。队友边界点原记录为16.005261米；合并先验保留首距下界后，同一点重评分为15.966125米。

默认允许10%上界容差时，原点案例的最短已访问候选将移动与检测时间降至193.857秒，几何上界为122.865米；边界案例为57秒、17.143米。这些只是在同一已访问集合中的折中点，不是连续域最快点。

精度敏感性：固定默认融合点，360边/0.1°的112.329304米在720边/0.05°下变为111.793188米；同时采用更细空间搜索可得到111.687447米。改变精度会改变上界和候选排序，尚未证明连续全局最优。

## 自动验证

执行 `python -m unittest discover -s problem2/tests -v`：**19项测试全部通过**，本轮约5.6秒。

- 4个安全点、9个径向值和101个角度值，共3636个组合，覆盖首距5米闭包、1000米分段和1500米上限；均满足与首收信相容的最小接收半径。
- 原有45组真源/固定读数误差组合，以及非原点旋转输入的后验包含与条带上界检查通过。
- 新增250个非网格读数，分别核验实际扇区被最近网格的增宽扇区覆盖，以及后验直径不超过几何上界。
- 新增四圆盘径向边界、超过1000米的安全薄层、移动预算、目标圆截断、原候选保留、最短近优点和非法精度输入检查。
- 修复并保留回归：容差附近的半平面裁剪不再把交点外推到原线段之外；首点在(1794,0)及(1794.9,0)的极薄边界先验，显示后验共用有效首距下界，直径不再超过报告的统一界。
- 示例JSON严格禁止NaN/Infinity且可重新解析，候选CSV已写出，SVG可按XML解析。

完整对比实验使用原点每策略1281个源组合、边界每策略252个源组合。对正常示向度分支再枚举5种固定误差，全部几何上界违反次数为零。安全策略全部收信；横移750米负例只有563/1281个原点源组合收信。这是确定性网格通过比例，不是实际成功概率。

## 复现和限制

```text
python problem2/solve.py
python problem2/compare_strategies.py
python -m unittest discover -s problem2/tests -v
python scripts/verify_project.py
```

全部默认路径相对于脚本文件；仓库外启动由统一验证入口再次核验。完整比较通常约一分钟量级，取决于本机负载；`compare_strategies.py --quick`只省略预算/精度/空间细化，默认写入独立的 `results/quick/`。固定比较案例参数若被改动会明确报错，避免比较口径静默漂移。

保证来自四圆盘推导、圆外包与半格增宽的集合包含关系；浮点计算和有限测试不是严格区间算术认证。1800米目标圆只约束源，机器人可在圆外。原点案例仍不能保证仅测向两次后就进入20米光学清除范围；覆盖范围应以最小覆盖圆判断，不能把直径的一半普遍当成覆盖半径。
