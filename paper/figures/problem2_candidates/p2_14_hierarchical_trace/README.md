# 图14的新候选：真实分层选点轨迹

对应论文5.2.5“评分组合与分层选点”。保留原图14，本目录给出三种新增呈现方式；14A已插入正文，正文副本为`../../p2_14a_progressive_zoom.pdf`，其余两版仍作候选。

| 版本 | 预览 | 矢量图 | 适用方式 |
| --- | --- | --- | --- |
| 14A 逐级放大 | [PNG](p2_14a_progressive_zoom.png) | [PDF](p2_14a_progressive_zoom.pdf)、[SVG](p2_14a_progressive_zoom.svg) | 优先推荐。虚线框连接下一层真实局部放大，适合解释加密过程并分辨密集点。 |
| 14B 同尺度分层 | [PNG](p2_14b_same_scale_layers.png) | [PDF](p2_14b_same_scale_layers.pdf)、[SVG](p2_14b_same_scale_layers.svg) | 三层各占一幅，坐标范围相同，直观看出新增区域逐渐收紧，保留完整上下两侧。 |
| 14C 两场景对照 | [PNG](p2_14c_two_scene_layers.png) | [PDF](p2_14c_two_scene_layers.pdf)、[SVG](p2_14c_two_scene_layers.svg) | 同尺度比较原点、边界两组自建算例，保留边界场景远端安全边界补点。 |

所有版本均采用蓝、金、绿区分第一、二、三层，颜色表示首次访问的层。星号表示该层评分精度下的优选点。14A中的灰色空心点是先前访问且仍保留的点。灰底为四圆盘收信保证与正常测向条件限定的安全候选域。

## 本次真实执行记录

直接调用当前生产求解器，临时记录其每一次候选点评分，在程序真实进度回调处划分层次；没有另写候选生成逻辑，也没有重排或人工收紧点的位置。

| 场景 | 第一层新增 / 累计 | 第二层新增 / 累计 | 第三层新增 / 累计 | 统一复核后的推荐点（局部米制坐标） |
| --- | --- | --- | --- | --- |
| 首点(0,0)，首次示向0° | 320 / 320 | 136 / 456 | 92 / 548 | (842.144, 546.895) |
| 首点(1500,0)，首次示向0° | 320 / 320 | 240 / 560 | 156 / 716 | (220.942, 195.473) |

三层移动距离/方位步长依次100米/5°、20米/1°、5米/0.25°；对应读数评分步长1°、0.5°、0.25°，最终统一复核为0.1°；圆外包360边、移动预算1100米。均与论文及生产默认参数一致。

14A和14C以 `b>0` 为固定展示规则，绘出该半域的全部新增点；已逐点核验另一侧真实访问了镜像点。原点各层画出160、68、46点，边界场景画出160、120、78点。没有随机挑选优良点，也没有移动点来消除重叠。14B完整展示全部320、136、92个新增点。

原点算例上半域新增点的横向/纵向跨度依次约992.43×831.00米、169.44×182.39米、35.01×37.40米。14A改变的是视窗范围，所有点仍使用原始坐标，且每个子图横纵保持等比例。

**层数增加时收紧的是新增加密网格，不是累计候选集。** 程序保留全部旧点，并继续加入所搜索方位的安全径向边界点。14C边界算例中，局部密集簇之外的远端有色点就是这些真实补点，不能删去后宣称全部候选形成单一紧簇。

**第三层临时优选点不等于最终推荐点。** 原点第三层优选为(865.280,509.689)，最终对548个点以0.1°重新评分后，选回第二层首次访问的(842.144,546.895)，评分上界112.3293米。三张图只展示候选生成与各层临时筛选，不把第三层星号标为最终推荐点。

## 核验与可复现来源

- [原点完整轨迹](../../../../problem2/results/hierarchical_trace_20260912/origin.json)
- [边界完整轨迹](../../../../problem2/results/hierarchical_trace_20260912/boundary.json)
- [运行摘要](../../../../problem2/results/hierarchical_trace_20260912/summary.json)
- 每张图的同名JSON保存图注、实际绘出点的ID、视窗和来源哈希。

每层计数与生产搜索历史一致；旧候选全部保留；每个点通过收信、正常测向和移动预算检查；两组逐层历史、最终选点、近优最短点均与当前已有结果一致；最终复核点集恰为三层访问并集。生产文件、既有结果均未改动。

新运行可执行 `python problem2/trace_hierarchical_search.py --output-directory results/hierarchical_trace_new_run`。已保存的轨迹拒绝覆盖。绘图执行 `python paper/plot_p2_hierarchical_trace.py`，默认只读取本次轨迹并重建本目录的三张候选图。路径均相对脚本解析，支持从仓库外部启动。

这些是自建输入下真实运行的离线结果，不是官方观测数据或正式测试。图中评分为最坏直径上界，不是真实定位误差；有限搜索不证明连续全局最优。

## 推荐图注（14A）

原点自建算例的分层候选搜索过程。蓝、金、绿点分别表示三层首次访问的候选，灰色空心点为保留的旧候选，星号表示本层优选点。图示上半域全部新增点，另一侧真实访问点与其镜像对应。虚线框及引线指示下一子图的放大范围；各子图采用原始局部坐标及横纵等比例。三层网格逐级加密后，仍对全部548个已访问候选采用统一读数精度复核，确定最终推荐点。
