# 问题 1：交会定位区域的直径与覆盖圆

**结论：先把每次示向度转成两个边界半平面的交，判断交集是否为空、是否无界；对非空有界交集取全部顶点，顶点两两距离的最大值就是区域直径。直径为这个值的圆，不一定能覆盖该定位区域。**

本目录已经提供完整推导、可运行程序、自建输入、结果和测试。依据是仓库 [`materials/problem_b/statement.pdf`](../materials/problem_b/statement.pdf) 第 1 页问题 1，以及第 2—3 页附录 2(1)、(3)。示向度从正东方向（`+x`）逆时针计量，单位为度，误差界为 `±1°`。

## 文件与运行

| 路径 | 用途 |
| --- | --- |
| `solve.py` | 标准库求解器、JSON 命令行入口及 SVG 绘图 |
| `examples/*.json` | 六组明确标记为自建的示例，其中两组吸收队友算例；不是官方测试数据 |
| `results/*.result.json` | 区域状态、顶点、面积、直径端点、直径圆、最小覆盖圆及 20 米覆盖判据 |
| `results/*.result.svg` | 有界示例的独立矢量示意图 |
| `tests/test_geometry.py` | 36 项几何、输入和异地运行检查，含 200 组卡壳/枚举精确对照 |

在仓库根目录运行（Python 3.10 及以上，无第三方求解依赖）：

```powershell
python -m problem1.solve
python -m unittest discover -s problem1/tests -v
```

也可以从任何目录直接运行本文件：

```powershell
python "D:\VS code\Repository\26-\problem1\solve.py" --demo
python "D:\VS code\Repository\26-\problem1\solve.py" --input examples/two_station_crossing.json --output results/custom.result.json
```

无参运行与 `--demo` 都会重建全部六组结果。`--input`、`--output` 的相对路径均相对于 **`problem1/`**，不受当前工作目录影响；绝对路径原样使用。输出文件会被同名重建，输入文件不被改写。

输入格式如下，坐标单位为米；`error_deg` 缺省为题设的 `1`：

```json
{
  "name": "自建示例",
  "data_origin": "Self-created example; not official simulator data.",
  "measurements": [
    {"x": 0, "y": 100, "bearing_deg": 0},
    {"x": 100, "y": 0, "bearing_deg": 90}
  ]
}
```

供后续问题调用的接口：

```python
from problem1.solve import Measurement, localize

result = localize([
    Measurement(0, 100, 0),
    Measurement(100, 0, 90),
])
print(result["status"], result["diameter_m"])
```

`analyze_halfplanes([HalfPlane(a,b,c), ...])` 则可直接处理一般约束 `a*x+b*y <= c`，用于几何扩展或边界情况验证。

此次合并保留原版 `Fraction` 几何判断，吸收队友 `b-question1-2/b12/geometry.py` 的旋转卡壳思路，并将面积比较和平局处理改为有理数精确比较。新增算例来自该项目 `examples/q1_triangle.json` 和 `examples/q1_two_bearings.json`，在各自 `data_origin` 中保留来源。没有引入队友以浮点容差认定平行的判据，因为这会把已有的 `1e-14` 近平行有界回归算例误判为无界。

## 1. 把示向度变为线性不等式

第 `i` 个检测点为 $S_i=(s_{ix},s_{iy})$，观测角为 $\theta_i$，误差界为 $\varepsilon=1^\circ$。设

$$
u_i^-=(\cos(\theta_i-\varepsilon),\sin(\theta_i-\varepsilon)),\qquad
u_i^+=(\cos(\theta_i+\varepsilon),\sin(\theta_i+\varepsilon)).
$$

记二维叉积 $[a,b]=a_xb_y-a_yb_x$。可能的源位置 $X=(x,y)$ 位于闭角扇区：

$$
[u_i^-,X-S_i]\ge0,\qquad [u_i^+,X-S_i]\le0.
$$

这两个条件可以直接展开成 `A*X <= b`：

$$
(u_{iy}^-,-u_{ix}^-)\cdot X\le (u_{iy}^-,-u_{ix}^-)\cdot S_i,
$$
$$
(-u_{iy}^+,u_{ix}^+)\cdot X\le (-u_{iy}^+,u_{ix}^+)\cdot S_i.
$$

全部扇区的交集 $P=\bigcap_i W_i$ 是闭凸多面集，非空有界时是凸多边形或其点、线段退化情形。不能直接对角度上下界做普通实数区间判断，因为 `359°` 与 `1°` 横跨零点；使用向量叉积自然处理这个问题。

程序额外加入 $(\cos\theta_i,\sin\theta_i)\cdot(X-S_i)\ge0$。对题设 `1°` 它是冗余约束；为测试而将 `error_deg` 设置为 `0` 时，它保证可行集是前向射线，而不是整条直线。通用接口只接受 `0 <= error_deg < 90`，实际题目保持 `1`。

**模型范围：**这里严格计算问题 1 与图 2 中由角度交会构成的“多边形定位区域”。若进一步和半径 `1800 m` 的目标圆、检测距离上界 `1500 m` 等信息求交，边界会包含圆弧；成功取得示向度还蕴含距离大于 `5 m`。这些额外信息没有被偷偷当成多边形约束。特别是，角度交集无界不表示源可能在实际目标圆外，只表示单靠这些角度还不能形成有界多边形。

## 2. 可行性、有界性和顶点

设总半平面数为 $m$（本实现是 $3n$）。算法如下。

1. 枚举每对不平行的边界直线，解二元线性方程求交点。
2. 对每个交点检查全部半平面，只保留可行交点；去重后取凸包。
3. 如果没有可行交点，再检查原点和原点到每条边界直线的正交投影，寻找可行点。仍未找到时，报告 `empty`。
4. 对非空交集，检查是否存在非零方向 $d$ 满足 $Ad\le0$。存在时报告 `unbounded`；否则交集有界。
5. 有界交集的凸包有 1、2、至少 3 个顶点时，分别报告 `point`、`segment`、`polygon`。

第三步保证不会把“没有顶点的非空半平面、带状区域、直线”误判为空。其完备性来自：非空闭凸集一定有距原点最近的点；这个点要么就是原点，要么是某条唯一有效边界的垂足，要么是两条独立边界的交点。

第四步使用**衰退方向**判据：如果 $X_0\in P$ 且 $Ad\le0$，那么所有 $X_0+td$（$t\ge0$）都属于 $P$。二维中，对每个非零法向量 $(a_j,b_j)$ 检查两条切向候选 $(-b_j,a_j)$ 和 $(b_j,-a_j)$ 即可：非平凡的二维闭凸锥总有边界方向；没有非零约束时，任意方向都可用。全过程无需预设有限边界框，也不会把超出某个框的区域误当作有限多边形。

程序用标准库 `Fraction` 对已构造的半平面系数进行精确的平行、可行性、凸包和衰退方向判断。**精确性仅针对所构造的半平面；输入浮点数和三角函数系数仍具有机器精度。**因此理论上恰好退化的角度数据可能因为三角函数舍入而出现极小几何扰动，不能把程序输出宣传成原始实数三角函数的符号计算。输出 JSON 顶点和长度最终转换为浮点数，可能显示约 `1e-13` 量级的舍入误差。

## 3. 为什么只需比较顶点距离

设有界非空定位区域的顶点为 $v_1,\ldots,v_k$。任意 $p,q\in P$ 可表示为顶点的凸组合：

$$
p=\sum_i\alpha_i v_i,\quad q=\sum_j\beta_j v_j,
\qquad \alpha_i,\beta_j\ge0,\quad \sum_i\alpha_i=\sum_j\beta_j=1.
$$

于是

$$
\|p-q\|=\left\|\sum_{i,j}\alpha_i\beta_j(v_i-v_j)\right\|
\le\sum_{i,j}\alpha_i\beta_j\|v_i-v_j\|
\le\max_{i,j}\|v_i-v_j\|.
$$

右端由一对顶点实际达到，因此

$$
\boxed{D(P)=\max_{i,j}\|v_i-v_j\|.}
$$

程序在逆时针凸包上使用旋转卡壳求最远点对。每条边依次寻找垂直距离最大的对面支撑点；随着边转动，对面指针只需沿凸包前进。若相邻两点到当前边的叉积相等，说明存在平行支撑边，必须同时检查两个端点。计算全程比较距离平方和叉积，最后一次开平方。

`diameter_squared_calipers` 的直径阶段为 $O(k)$；保留独立的 $O(k^2)$ 顶点对枚举 `diameter_squared_bruteforce` 作为测试对照。顶点枚举和约束检查仍为 $O(m^3)$，凸包为 $O(k\log k)$，附加最小覆盖圆仍为 $O(k^4)$，**完整求解没有因此变成线性复杂度**。这些是算术操作次数，`Fraction` 的位复杂度另计。检测点很多时仍需进一步优化半平面交和最小覆盖圆。

空集没有本程序定义的直径，输出 `diameter_m: null`。无界非空集直径为正无穷，输出 `status: "unbounded"`、`diameter_is_infinite: true` 以及 `diameter_m: null`，避免写入非标准 JSON 的 `Infinity`。单点直径为 `0`，线段直径为端点距离。

## 4. 直径为 D 的圆不一定覆盖定位区域

设 $a,b$ 是一对最远顶点，$\|a-b\|=D$。如果某个半径为 $D/2$ 的闭圆盘能够同时覆盖这两个点，由三角不等式的等号条件，其圆心只能是

$$
c_D=(a+b)/2.
$$

因此无需搜索所有圆心：检查所有顶点是否都满足 $\|v_i-c_D\|\le D/2$，就能回答是否**存在**直径为 $D$ 的覆盖圆。圆盘是凸集，覆盖全部顶点也就覆盖整个凸多边形。

反例并非任意画一个三角形，而是可以由题设的 `±1°` 示向度实际构造。取

$$
A=(0,0),\ B=(20,0),\ C=(10,10\sqrt3),
$$

并使用以下检测信息：

| 检测点 | 坐标（米） | 示向度 |
| --- | --- | --- |
| $S_1$ | $(-1000,0)$ | $1^\circ$ |
| $S_2$ | $(520,-500\sqrt3)$ | $121^\circ$ |
| $S_3$ | $(510,510\sqrt3)$ | $241^\circ$ |

三组扇区的下边界方向分别为 `0°`、`120°`、`240°`，恰好是等边三角形三条有向边。每个检测点都位于对应边起点的反向延长线上，距起点 `1000 m`。对每组扇区，三角形相对于该边的最大偏角为

$$
\arctan\frac{10\sqrt3}{1010}\approx0.9825^\circ<2^\circ,
$$

故另一条边界是冗余约束，三扇区交集就是这个等边三角形。可以把真实源放在重心 $(10,10\sqrt3/3)$；三次观测都符合 `±1°`，三站到源约 `1010 m`，与题目允许的例如 `1200 m` 接收半径兼容，源也在 `1800 m` 目标圆内。

该区域的直径为 $D=20$，但其最小覆盖圆半径为 $R=20/\sqrt3\approx11.5470>D/2=10$，所以直径为 `20 m` 的任何圆都不能覆盖它。最小覆盖圆直径约为 `23.0940 m`。

## 5. 如果需要保证全覆盖，应计算最小覆盖圆

平面有限点集的最小覆盖圆由至多三个边界点确定：一个点、两个对径点，或者三个点的外接圆。程序枚举每对顶点的直径圆、每组三个非共线顶点的外接圆，保留覆盖全部顶点且半径最小者；单点单独处理。这直接得到整个定位多边形的最小覆盖圆。

该附加算法最坏为 $O(k^4)$，适合本题少量检测点产生的少量顶点；它并非面向大规模点云的最快实现。其圆心、半径平方和覆盖比较均采用有理数计算；仅最终半径开平方为浮点。

若后续需要在一个位置对区域内任意可能源实施题设 `20 m` 的光学定位，判据应为**最小覆盖圆半径不超过 `20 m`**。仅有 `D <= 40 m` 不是充分条件。

### 5.1 更直接的 40 米反例与直径充分判据

吸收队友的第二个等边三角形构造：令 $A=(0,0)$、$B=(40,0)$、$C=(20,20\sqrt3)$，三站为 $(-1000,0)$、$(540,-500\sqrt3)$、$(520,520\sqrt3)$，示向度仍为 `1°`、`121°`、`241°`。另一条扇区边界对三角形的最大偏角为

$$
\arctan\frac{20\sqrt3}{1020}\approx1.945^\circ<2^\circ,
$$

因此仍能由题设三组 `±1°` 观测形成该三角形。取真实源为重心 $(20,20\sqrt3/3)$，各站到源约 `1020 m`，与 `1200 m` 接收半径兼容。此时

$$
D=40\text{ m},\qquad R_* = \frac{40}{\sqrt3}\approx23.0940\text{ m}>20\text{ m}.
$$

这直接证明：即使定位区域直径已经达到 `40 m`，也不能保证存在一个位置对全区域实施 `20 m` 光学定位。最远点对的中点更差，其到第三个顶点的距离约为 `34.6410 m`。

若只有区域直径，也可以给出不依赖形状的界：

$$
\boxed{\frac D2\le R_*\le\frac D{\sqrt3}.}
$$

下界来自任何覆盖圆必须容纳最远点对。上界可直接由最小覆盖圆的支撑点证明：两点支撑时 $R_*=D/2$；三点支撑时其圆心位于支撑三角形内或边界上，否则可移动圆心缩小半径。支撑三角形的最大角 $A$ 满足 $60^\circ\le A\le90^\circ$，对应边 $a\le D$，由正弦定理 $R_*=a/(2\sin A)\le D/\sqrt3$。

所以 `D <= 20√3 ≈ 34.6410 m` 是 `20 m` 全覆盖的充分条件；`D <= 40 m` 只是必要条件。对中间区间必须检查实际最小覆盖圆。例如直径 `40 m` 的线段半径恰为 `20 m`，仍可覆盖。两条直径阈值都不是替代实际覆盖圆计算的理由。

### 5.2 新增结果字段如何读

`area_m2` 是角度交集面积，点和线段为 `0`；面积不能替代直径，因为很细长的区域面积小但最远距离仍可很大。`max_distance_from_diameter_center_m` 给出最远点对中点到区域的最大距离，用于说明直接取这个中点的风险。

`optical_localization` 在有界非空交集上输出：

| 字段 | 含义 |
| --- | --- |
| `guaranteed_from_enclosing_center` | 最小覆盖圆半径平方是否不超过 `400`；为真时，可在该圆心覆盖整个角度区域 |
| `radius_margin_m` | `20 - R*`，负数表示还差多少米才能满足半径要求 |
| `diameter_only_sufficient` | 是否满足 `D² <= 1200`，即充分条件 `D <= 20√3` |
| `diameter_only_necessary` | 是否满足必要条件 `D² <= 1600`；为真本身不能保证全覆盖 |

所有阈值判断在有理数平方上完成，不给 `20 m` 阈值额外放宽浮点容差；它们仍仅针对机器三角系数构造的区域。例如理论恰为 `40 m` 的三角形会输出 `40.00000000000003`，使必要条件字段为假，这是明确保留的数值边界，不影响该例最小覆盖半径明显超过 `20 m` 的结论。空集和无界交集的这些新增有限区域指标为 `null`，须结合 `status` 解读。这些字段只判断几何覆盖，不表示已经执行移动、光学定位或清除。

## 已生成结果和验证

| 自建示例 | 状态 | 直径（米） | 最小覆盖圆半径（米） | 直径为 D 的圆能否覆盖 |
| --- | --- | ---: | ---: | --- |
| `two_station_crossing` | 有界四边形 | 4.9385425824 | 2.4692712912 | 能 |
| `triangle_counterexample` | 有界三角形 | 20.0000000000 | 11.5470053838 | 不能 |
| `triangle_optical_counterexample` | 有界三角形 | 40.0000000000 | 23.0940107676 | 不能 |
| `wide_baseline_crossing` | 有界四边形 | 31.4796747478 | 15.7398373739 | 能 |
| `parallel_unbounded` | 无界 | 正无穷 | 不存在有限覆盖圆 | 不适用 |
| `inconsistent_measurements` | 空集 | 未定义 | 不适用 | 不适用 |

本目录的 36 项测试覆盖空集、无界半平面/带/直线/扇区、点、线段、跨零角、同向平行观测、`1e-14` 近平行产生的巨大有限三角形、`1e12` 坐标平移、两组等边三角形实现场景、随机一致观测、添加观测后的直径单调性、非法输入，以及从仓库外工作目录调用程序。新增 200 组随机凸包和精确平行支撑边的卡壳/枚举对照，以及线段长度恰为 `40 m` 和再多 `1e-12 m` 时的覆盖边界检查。

可从仓库根目录额外运行 `python scripts/verify_reference.py`，使用 SciPy/HiGHS 的线性规划结果独立核对几何状态与方向支持值，报告写入 `validation/reference_geometry.json`；这是可选第三方交叉验证，第一问正常运行仍只需标准库。全部输入和结果都是离线自建验证，没有调用官方模拟器，也没有消耗正式测试机会。
