# 问题3：完整圆域均衡本地样例

共7000例，10至16源各1000例。按用户2026年9月13日确认的公开规则重建，旧balanced_normal_v2保留。

- 每个源独立按面积均匀分布于半径1800米的完整目标圆，r=1800√U；无构造难度变量，不缩小空间范围。
- 接收半径独立均匀分布于1000至1500米；从1至20频道无放回均匀抽取N个。
- 第三问全部全向；第四问定向源数在1至N-1中离散均匀抽取，源身份等概率选择，朝向独立均匀分布于0至360度。
- 误差由样例种子、频道和位置固定为hash_uniform，范围±1度，返回角度保留两位小数。同位置重复测量读数不变。
- 初始位置(0,0)、初始频道1、速度5米/秒，测量5秒，测量切频道1秒，失败clear为3秒、成功为5秒，clear不切换测向频道。近距离阈值5米，清除阈值20米。

均匀抽样及hash_uniform是明确的本地建模假设；官方未公布场景和误差分布，不能称为官方同分布、官方演练或正式成绩。模拟器只复现公开物理与动作规则；本地时间使用双精度累计、响应保留6位小数，官方内部以微秒累计，因此不主张实现逐微秒完全一致。

策略只接收公开响应，自身位置、频道由已发送动作推导；不传源数、坐标、接收半径、朝向、种子或样例标识。新当前批测工作进程拒绝读取仓库数据文件。真值只由外部仿真器生成反馈和事后核验使用。

种子域、生成器指纹和文件校验值见manifest.json。新种子与历史均衡数据分离；本批不用于调参，不宣称独立留出集。生成和校验不会连接平台，已有数据损坏时拒绝覆盖。
